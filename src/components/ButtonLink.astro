---
import { twMerge } from "tailwind-merge"

const { href, class: className } = Astro.props
---

<a
  href={href}
  class={twMerge("inline-block text-xl font-semibold px-6 py-2.5 rounded-full bg-brand-500 hover:bg-brand-600 text-white", className)}
>
  <slot />
</a>
